import 'package:flutter/material.dart';

const quizzSecondaryColor = Color(0xFF8B94BC);
const quizzGreenColor = Color(0xFF6AC259);
const quizzRedColor = Color(0xFFE92E30);
const quizzGrayColor = Color(0xFFC1C1C1);
const quizBlackColor = Color(0xFF101010);
const quizzPrimaryGradient = LinearGradient(
  colors: [Color(0xFF46A0AE), Color(0xFF00FFCB)],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

const double quizzDefaultPadding = 20.0;
